import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette:{
    primary: {
      main: '#254138',
      light: '#32574b',
      dark: '#12201c'
    },
    secondary: {
      main: '#ff9d22'
    }
  }
})